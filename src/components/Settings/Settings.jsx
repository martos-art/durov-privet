import React from 'react';
import sett from './Settings.module.css';

const Settings = (props) => {
    return (
        <div className={sett.content} >
            Settings
            
        </div>
    )
};

export default Settings;